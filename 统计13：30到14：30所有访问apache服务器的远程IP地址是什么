#!/bin/bash
#awk 使用-F选项指定文件内容的分隔符是/或者：
#条件判断$7:$8大于等于13：30，$7:$8小于等于14：30
#日志文档内容里面，第一列是远程主机的IP地址，使用awk单独显示第一列即可
awk -F "[/:]" '$7":"$8>="13:30" && $7":"$8<="14:30"{print $1}' /var/log/httpd/access_log
